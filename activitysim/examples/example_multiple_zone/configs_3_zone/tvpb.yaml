CONSTANTS:
  c_ivt: 1
  c_wait: 1.5
  c_walk: 1.7
  c_cost: -0.002678108
  C_UNAVAILABLE: -999
  c_auto_operating_cost_per_mile: 18.29
#  c_walk_fpm: 264

DEMOGRAPHIC_SEGMENTS:
  high_income:
    vot: -0.032
  low_income:
    vot: -0.025

MAZ_TAP_MODES:
  walk:
    SPEC: tvpb_walk_maz_tap.csv
    CHOOSER_COLUMNS:
      - walk_time
  drive:
    SPEC: tvpb_drive_maz_tap.csv
    CHOOSER_COLUMNS:
      - drive_time
      - DIST

TAP_TAP:
  SPEC: tvpb_tap_tap.csv
  TIME_OF_DAY: 'AM'
  PREPROCESSOR:
    SPEC: tvpb_tap_tap_annotate_choosers_preprocessor
    DF: df
