Description,Target,Expression
#,,
# define segment_type to use for slicing choosers with a value that identifies the spec to be used ,,
# e.g. univ segment means there will be a spec called stop_frequency_univ.csv,,
# so the 'school' tour_type can treat univ and non-univ school tours differently,,
#,segment_type,"df.tour_type.where((df.tour_type != 'school') | ~df.is_university, 'univ')"
#,segment_type,"segment_type.where((df.tour_category!='subtour'), 'subtour')"
,ptype,"reindex(persons.ptype, df.person_id)"
,person_type,ptype.map(constants.PTYPE_NAME)
,start,"reindex_i(tours.start, df.tour_id)"
